var escapeMap = {
    "<": "&#60;",
    ">": "&#62;",
    '"': "&#34;",
    "'": "&#39;",
    "&": "&#38;"
};

var escapeFn = function (s) {
    return escapeMap[s];
};

module.exports = function (content) {
    return ('' + content)
        .replace(/&(?![\w#]+;)|[<>"']/g, escapeFn);
};