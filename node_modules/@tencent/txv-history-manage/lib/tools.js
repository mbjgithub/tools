module.exports = {
  by: function (attr) {
    return function (a, b) {
      var tempA = parseInt(a[attr]) || 0;
      var tempB = parseInt(b[attr]) || 0;
      return tempA < tempB ? 1 : -1
    }
  }
}